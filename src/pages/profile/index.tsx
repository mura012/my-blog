import { MyProfile } from "src/components/MyProfile";
import { Layout } from "src/layout";

const Profile = () => {
  return (
    <Layout title="プロフィール">
      <MyProfile />
    </Layout>
  );
};

export default Profile;
